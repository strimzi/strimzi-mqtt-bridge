package io.strimzi.kafka.bridge.mqtt.config;

import java.util.Map;
public class BridgeConfig extends AbstractConfig {
    public BridgeConfig(Map<String, Object> config) {
        super(config);
    }
}
